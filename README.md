# ICSI536-ML-Project

# Project Directory Strucutre
```
image-classification/
├── data/
├── src/
│   ├── __init__.py
│   ├── dataset.py        # Load and preprocess datasets
│   ├── neural_network.py  # Define the neural network class
│   ├── layers.py         # Define layers (e.g., Dense, Activation)
│   ├── loss.py           # Define loss functions
│   ├── optimizer.py       # Define optimizers
│   └── utils.py          # Utility functions (e.g., accuracy calculation)
├── main.py               # Main script to run the training
├── requirements.txt      # Dependencies (if any)
└── README.md             # Project description and instructions
```